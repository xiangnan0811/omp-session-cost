## omp-session-cost 0.8.0

这一版修复数据、归因与复制分析链路，不改模型或工作流配置。

### 全面诊断，而非预设优化方向

复制提示词先核验数据质量，再开放式分析任务推进、协作、失败重试、并发等待、上下文缓存、费用分布及此前未注意的异常。用户的额外关注点不会把诊断重新限制在旧的配置问题上。

### 数据与可分析性

- 识别 OMP 子任务包装模板，修复无意义标题及任务哈希关联。通过原始 task 结果 ID／工具调用关联实际子实例；不按返回时间改写归属，不强分歧义候选。
- 父实例不再用当前实例自身填充；跨任务主控展示各任务分布，不只显示首个任务。增加各类 Token、调用、金额及展示总计的逐维度守恒校验。
- 逐实例展示采集起止、实际钩子活动、缺口原因与健康记录。晚加载、无请求钩子事件、无法匹配分别可见，不补造历史耗时。
- Advisor 上下文观察不依赖请求钩子；缺少处置标为未知，不再算作明确 open。默认保留凭据脱敏的建议正文、原名及来源证据。范围外但用于解释关联的证据随报告携带。
- 数据质量、时序和 Advisor 证据提前；默认报告减少重复展开，完整 Markdown／JSON 保留全量明细。

### 复制和文件完整性

Markdown 首尾标记包含正文长度与 SHA-256，JSON 增加完整性元数据。原生剪贴板回读核对，回读不符报错；无法回读或 OSC 52 只报告已发送、未验证。预览按 `s` 保存完整文件，保存后回读核对，不覆盖已有文件。

`npm run verify:report -- /path/to/report.md` 可核验转交后的 Markdown 或 JSON。传输完整、账本守恒、源数据完整是三个独立条件，不互相代替。

### 正常升级

```sh
omp plugin install "github:xiangnan0811/omp-session-cost#v0.8.0"
```

重启 OMP 后执行 `/cost`。保持现有半屏布局、主题、快捷键、命名基线及原始名称。无需手工覆盖文件；Release 示例全部为合成数据，不包含真实会话。

JSON 格式升级到 v4，运行时 sidecar 仍兼容 schema 1，历史基线可继续读取。历史未采集事件不能恢复；这次没有拿当前配置回填历史。验证包含 Node 测试、语法检查、20,000 条调用合成基准、隔离打包导入／注册检查，以及 CI Bun／SQLite 集成。

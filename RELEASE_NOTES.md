# omp-session-cost 0.9.0

本版修复 0.8.0 报告暴露的采集、关联与导出问题，格式升级为 v5。不更改模型配置、思考等级、独立双审、Advisor 启用状态或原有半屏界面。

## 修复范围

- 请求跟踪由单槽位改为有界模型／提供商／回合作用域集合；主控与不同模型辅助请求不互相覆盖，压缩遗留请求不污染下一回合；同作用域同模型冲突保留歧义。响应头和完成时刻分开。
- 捕获嵌套工具的父事件、实际 task 结果及 eval 状态；读取结构化 workpool push 与初始化 pool/batch。跨父任务复用而无 item 身份的池不按时间强行分摊。
- 区分初始任务、后续任务段和最新生命周期指令，避免最终清理标题覆盖整场历史消耗。
- 复制报告含全量精简用量账本、压缩父引用图、源文件定义和按异常／主体分层选样的详细证据；失败原因按凭据语法脱敏后保留。
- 增加离线算术重放、调用集合及引用声明检查；完整 JSON 的明细与重放账本交叉检查。原始 NUL／控制字符在传输校验前转义。
- 配置内容去重且保留每次观察身份；恢复时从请求重新观察提示／工具指纹，未知不使用导出时配置回填。
- 分开实际活动时间与观察器状态采样。eval 内部等待不可见、压缩自身用量未知、辅助请求未关联分别展示。
- Advisor 按建议与卡片分别计数，记录明确替代关系；有效费率从日志类别金额反算，定价规则未记录和缺少独立价格来源单列，不冒充官方价格核验。

## 验证与边界

新增合成回归覆盖主控／Advisor 交错、同模型歧义、压缩、恢复、嵌套 task、workpool 跨任务、标题、传输控制字节、离线重放与包内导入。六个关键兼容回归在原始 0.8.0 上均失败，在本版均通过。发布工作流执行 Node 全量测试、Bun SQLite／注册检查、示例 Markdown／JSON 离线验证及发布包校验。

这不是用户真实会话的新采集验收。旧日志未观察到的时间和用量不能补造；原生扩展钩子尚未提供的辅助请求完成／归属、同模型并发身份及跨任务池 item 归属继续标明未知，不宣称 100% 覆盖或修改 OMP 本体来绕过。

## 升级

```sh
omp plugin install "github:xiangnan0811/omp-session-cost#v0.9.0"
```

重启所有需要使用新版采集器的 OMP 进程，通过 `/cost` 确认版本。现有会话日志保持原样；新采集从新版加载时开始。公开示例与测试全部是合成数据，发布不包含用户会话。
